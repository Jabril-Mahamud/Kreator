# Kreator Frontend

Minimal Next.js 14 (App Router, TypeScript) UI for the Kreator backend.

## Local development

```bash
cp .env.local.example .env.local   # edit if your backend runs elsewhere
npm install
npm run dev
```

Open http://localhost:3000.
