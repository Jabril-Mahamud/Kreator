# Env files
.env
.env.local
.env*.local

# Secrets — never commit raw secrets
secrets/raw/

# Sealed secrets are cluster-specific (encrypted with the controller's
# active keypair). bootstrap.sh regenerates them on every `make up`.
secrets/sealed/

# Kind cluster config (auto-generated)
kubeconfig

# Docker
.docker/

# Node
node_modules/
.next/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# IDE
.vscode/
.idea/
.claude/

# OS
.DS_Store
Thumbs.db
