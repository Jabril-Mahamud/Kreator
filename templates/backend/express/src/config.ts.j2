export const config = {
  port: parseInt(process.env.PORT || "8000", 10),
  databaseUrl:
    process.env.DATABASE_URL ||
    "postgresql://postgres:postgres@localhost:5432/{{ name | replace('-', '_') }}",
  nodeEnv: process.env.NODE_ENV || "development",
};
