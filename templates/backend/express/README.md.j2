# {{ name }} backend

Express + TypeScript backend for {{ name }}.

## Local development

```bash
npm install
npm run dev
```

The server starts on http://localhost:8000. API docs are available at the `/api/items` endpoints.

## Environment variables

Copy `.env.example` to `.env` and configure `DATABASE_URL`.
