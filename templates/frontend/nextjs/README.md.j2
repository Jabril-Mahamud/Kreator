# {{ name }} frontend

Next.js 14 frontend for {{ name }}.

## Local development

```bash
npm install
npm run dev
```

Open http://localhost:3000 to view the app.

## Environment variables

Copy `.env.local.example` to `.env.local` and set `NEXT_PUBLIC_API_URL` to your backend URL.
