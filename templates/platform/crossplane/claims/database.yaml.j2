apiVersion: kreator.dev/v1alpha1
kind: Database
metadata:
  name: {{ name }}-db
  namespace: {{ name }}
spec:
  parameters:
    name: {{ name }}-db
    namespace: {{ name }}
    size: small
  compositionSelector:
    matchLabels:
      provider: {{ provider }}
