apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ name }}-{{ frontend_name }}
  namespace: argocd
spec:
  project: {{ name }}
  source:
    repoURL: {{ repo_url }}
    targetRevision: HEAD
    path: deploy/helm/{{ frontend_name }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ name }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
