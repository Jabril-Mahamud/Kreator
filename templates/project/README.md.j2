# {{ name }}

Generated by [Kreator](https://github.com/JabrilMahamique/Kreator).

## Stack

{% if frontends | length == 1 %}
- Frontend: {{ frontends[0].template }}
{% else %}
- Frontends:
{% for fe in frontends %}
  - {{ fe.name }}: {{ fe.template }} ({{ fe.platform }})
{% endfor %}
{% endif %}
- Backend: {{ backend }}
- Database: {{ database }}
- Provider: {{ provider }} ({{ region }})

## Quick start

```bash
# Local development (Kind cluster with ArgoCD + Crossplane)
make dev

# Deploy to {{ provider }}
make deploy

# Tear down
make destroy
```

## Local URLs

After `make dev`:

{% for fe in frontends if fe.platform == "web" %}
- {{ fe.name }}: http://{{ fe.name }}.localhost:9080
{% endfor %}
- Backend: http://api.localhost:9080
- ArgoCD: http://argocd.localhost:9080 (log in with `{{ name }} / admin123` to see just this project, or `admin / admin123` for everything)
{% for fe in frontends if fe.platform == "mobile" %}

The {{ fe.name }} app is not deployed locally. Run `cd apps/{{ fe.name }} && npx expo start`.
{% endfor %}

## Project structure

```
{{ name }}/
  kreator.yaml          # Project config
  apps/
{% for fe in frontends %}
    {{ fe.name }}/{{ " " * (16 - fe.name | length) }}# {{ fe.template }} app ({{ fe.platform }})
{% endfor %}
    backend/            # {{ backend }} app
  infrastructure/       # Crossplane XRDs, compositions, claims
  deploy/
    argocd/             # ArgoCD app-of-apps
    helm/               # Helm charts for web frontends and backend
  secrets/              # Sealed Secrets
```
