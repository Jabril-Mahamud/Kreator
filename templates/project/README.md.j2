# {{ name }}

Generated by [Kreator](https://github.com/JabrilMahamique/Kreator).

## Stack

{% if frontends | length == 1 %}
- Frontend: {{ frontends[0].template }}
{% else %}
- Frontends:
{% for fe in frontends %}
  - {{ fe.name }}: {{ fe.template }} ({{ fe.platform }})
{% endfor %}
{% endif %}
- Backend: {{ backend }}
- Database: {{ database }}
- Provider: {{ provider }} ({{ region }})

## Quick start

```bash
# Local development (Kind cluster with ArgoCD + Crossplane)
make dev

# Deploy to {{ provider }}
make deploy

# Tear down
make destroy
```

## Project structure

```
{{ name }}/
  kreator.yaml          # Project config
  apps/
{% for fe in frontends %}
    {{ fe.name }}/{{ " " * (16 - fe.name | length) }}# {{ fe.template }} app ({{ fe.platform }})
{% endfor %}
    backend/            # {{ backend }} app
  infrastructure/       # Crossplane XRDs, compositions, claims
  deploy/
    argocd/             # ArgoCD app-of-apps
    helm/               # Helm charts for web frontends and backend
  secrets/              # Sealed Secrets
```
